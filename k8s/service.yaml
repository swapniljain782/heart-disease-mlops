apiVersion: v1
kind: Service
metadata:
  name: heart-disease-api-service
spec:
  type: LoadBalancer
  externalIPs:
    - 20.17.177.233  # <-- Replace with your Azure VM Private IP
  selector:
    app: heart-disease-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8000
